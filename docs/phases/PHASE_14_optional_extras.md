# Phase 14 — Optional extras (only with the human's go-ahead)

### [ ] P14-T01 — BrawlCrate bridge plugin
BrawlAPI (IronPython) loader/plugin in its own folder: menu "Track Studio → Re-import course model
from project" and "Export material presets to project", reading a job JSON the app writes. Windows
testing by the human required; keep it optional and self-contained.
### [ ] P14-T02 — Blender live link (if P6-T07 was deferred)
### [ ] P14-T03 — macOS evaluation
Run the test suite and a packaged build on macOS (CI `macos-latest`); list gaps; decide support level.
